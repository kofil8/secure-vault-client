"use client"; // Ensures that this component runs on the client side

import WebViewer from "./webviewer"; // Import WebViewer component

export default function Page() {
  return (
    <div>
      <WebViewer />
    </div>
  );
}
